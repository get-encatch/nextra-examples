{
  "name": "encatch-nextra-examples",
  "private": true,
  "scripts": {
    "dev:nextjs": "pnpm --filter nextjs dev",
    "build:nextjs": "pnpm --filter nextjs build",
    "test:visual": "playwright test",
    "test:visual:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.51.0"
  }
}
